[project]
name = "pySAFER"
version = "0.1.0"
# ... 其他配置保持不变 ...

dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "scipy",
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["pySAFER"]